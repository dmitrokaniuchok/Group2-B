description: OK
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 200
        message:
          type: string
          example: 'Recipes fetched successfully'
        data:
          type: object
          description: 'Returned data depends on endpoint'
